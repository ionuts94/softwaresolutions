export type p = {
  s: string
}

export interface CompanyData {
  cui: string;
  name: string;
  address: string;
  county: string,
  nrRegCom: string;
  postCode: string;
  phoneNumber: string;
  registrationDate: string,
  addedToQueDate: Date
}